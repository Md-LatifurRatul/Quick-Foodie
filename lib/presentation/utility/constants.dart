import 'package:flutter/material.dart';

class Constants {
  static const Color loginBg = Color(0xFF00C470);
  static const Color signupBg = Color(0xFF000A54);
  static const double defpaultPadding = 16.0;
  static const Duration defaultDuration = Duration(microseconds: 300);
}
